{
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "standard": {
    "ignore": [
      "dist/",
      "tmp/",
      "scripts/",
      "test/"
    ]
  },
  "dependencies": {
    "phaser": "^4.0.0",
    "vite": "^8.0.10"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "standard": "^17.1.2",
    "vitest": "^3.2.6"
  }
}
